name: First Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print outputs
        run: |
          echo "First output!"
          echo "Second output!"
      - name: Print goodbye
        run: echo "Done - bye!"
